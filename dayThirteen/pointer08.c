// passing an array as function argumnets 
// Important :- passing an array in a function by using funcName(dataType array[]) won't really pass the whole array instead It creates pointer variable of same name. Compiler copies the address of first element of the array  funcName(dataType array[])==> funcName(dataType *array). Arrays are passed as refrence parameters

